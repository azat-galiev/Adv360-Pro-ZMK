macro_switch_to_ru: macro_switch_to_ru {
    compatible = "zmk,behavior-macro";
    label = "macro_switch_to_ru";
    #binding-cells = <0>;
    wait-ms = <1>;
    bindings
        = <&macro_press &kp LEFT_ALT &kp LEFT_SHIFT>
        , <&macro_tap &kp N1>
        , <&macro_release &kp LEFT_SHIFT &kp LEFT_ALT>
        ;
};

macro_switch_to_en: macro_switch_to_en {
    compatible = "zmk,behavior-macro";
    label = "macro_switch_to_en";
    #binding-cells = <0>;
    wait-ms = <1>;
    bindings
        = <&macro_press &kp LEFT_ALT &kp LEFT_SHIFT>
        , <&macro_tap &kp N0>
        , <&macro_release &kp LEFT_SHIFT &kp LEFT_ALT>
        ;
};

fn_or_switch_to_en: fn_or_switch_to_en {
    compatible = "zmk,behavior-hold-tap";
    label = "fn_or_switch_to_en";
    #binding-cells = <0>;
    flavor = "tap-preferred";
    tapping-term-ms = <200>;
    bindings = <&mo 2>, <&macro_switch_to_en>;
}

fn_or_switch_to_ru: fn_or_switch_to_ru {
    compatible = "zmk,behavior-hold-tap";
    label = "fn_or_switch_to_ru";
    #binding-cells = <0>;
    flavor = "tap-preferred";
    tapping-term-ms = <200>;
    bindings = <&mo 2>, <&macro_switch_to_ru>;
}
